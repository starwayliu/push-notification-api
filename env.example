# Server Configuration
PORT=3000
NODE_ENV=development

# Web Push (VAPID) - Required for Web notifications
# Generate keys using: npm install -g web-push && web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_SUBJECT=mailto:your-email@example.com

# Android FCM - Required for Android notifications
# Get credentials from Firebase Console > Project Settings > Service Accounts
FCM_PROJECT_ID=your_firebase_project_id
FCM_PRIVATE_KEY=your_firebase_private_key
FCM_CLIENT_EMAIL=your_firebase_client_email
